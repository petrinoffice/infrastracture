apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: dev-helm
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/petrinoffice/infrastracture.git
    targetRevision: HEAD
    path: helm/charts/standard-service
    helm:
      parameters:
      values: |-
        app:
          name: test-app-helm-dev
          port: 8080
          image:
            name: test
            tag: 0.0.1
        replicas: 1

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
